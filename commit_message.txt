🧹 [Code Health] Clean up unused React import in SearchBar

🎯 **What:** Removed the unused `import * as React from "react";` import statement from `src/components/SearchBar.tsx`.
💡 **Why:** This resolves a dead code/unused import issue, which improves code readability and maintainability without affecting any functionality, as `React` was not being used anywhere in the file.
✅ **Verification:** Verified by running `npm run lint` successfully and executing the full Playwright E2E test suite (`npx playwright test`) to ensure no regressions were introduced.
✨ **Result:** A cleaner component file with improved code health.
