⚡ Bolt: Cache MCP tools findMany queries

🎯 What: Wrapped the Prisma tool findMany queries within unstable_cache in src/app/api/mcp/route.ts.
💡 Why: Reduces duplicate heavy db calls over frequent polling for tools lists via the SSE connection, leading to a performance optimization.
✅ Verification: Ran `npm run lint` and `npx playwright test` (E2E API test suite passed up to DB transfer limit error, which is unrelated to this PR's changes).
✨ Result: `findMany` queries are correctly cached with `unstable_cache` handling Prisma Date object serialization limitations smoothly.
